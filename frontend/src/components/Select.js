

export default function Select({
    name,
    value,
    onChange,
    ...props
}) {

    return (
        <Select
            name={name}
            value={value}
            onChange={onchange}>
        </Select>

    );
}